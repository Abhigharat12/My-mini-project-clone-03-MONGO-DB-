<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>WhatsApp Messages</title>
  <link rel="stylesheet" href="styles.css">

</head>
<body>
    <body>

  <h4 style="text-align: center; background-color: #333; color: white; padding: 10px; margin: 0;">
    Created by Abhishek Gharat.
  </h4>

  <!-- Rest of your content -->


  <h3>All Chats</h3>

<form method="GET" action="/chats/new" class="new-chat-form">
  <button type="submit">➕ New Chat</button>
</form>


<% for(let chat of chats) { %>
 <div class="chat" style="position: relative; padding-bottom: 50px; border: 1px solid #ccc; margin-bottom: 15px; border-radius: 8px; padding: 15px;">

  <!-- Your chat content here -->
  <p><strong>From:</strong> <%= chat.from %></p>
  <p><strong>To:</strong> <%= chat.to %></p>
  <div class="message">
    <p><strong>Message:</strong> <%= chat.message %></p>
  </div>
  <p class="meta"><strong>Date:</strong> <%= chat.created_at.toLocaleString() %></p>

  <!-- Buttons container -->
<div style="position: absolute; bottom: 10px; right: 15px;">

  <form method="GET" action="/chats/<%= chat._id %>/edit" class="edit-chat-form" style="display: inline-block; margin-right: 8px;">
    <button type="submit" style="padding: 5px 12px; background-color: #007bff; color: white; border: none; border-radius: 4px; cursor: pointer;">
      ✏️ Edit
    </button>
  </form>

  <form method="POST" action="/chats/<%= chat._id %>?_method=DELETE" class="delete-chat-form" style="display: inline-block;">
    <button type="submit" style="padding: 5px 12px; background-color: #dc3545; color: white; border: none; border-radius: 4px; cursor: pointer;">
      🗑️ Delete
    </button>
  </form>

</div>

</div>

<% } %>


</body>
</html>
